# mtandaolabsEdu — Full System QA Report (tester run)

**Date:** 2026-09-05 (night)
**Tester:** Krish (acting as tester per Braxton)
**URL:** https://skuul.mtandaolabs.com
**Method:** live browser walkthrough (Playwright, desktop 1440×900) as super admin → create school → enter school → add staff/students/parents → sweep all modules. Console errors + HTTP failures captured per page.

## Flow checklist
- [x] 1. Super admin login — 200, clean, 0 console errors
- [x] 2. Create school ("QA Test School") — success banner, form clears, record in DB
- [x] 3. Enter school (set working school) — ⚠️ UI button dead (F1); backend POST works, full menu appears, banner shows school + Exit link
- [ ] 4. Create admin
- [x] 5. Create teacher ("Mary Wanjiku") — ⚠️ UI blocked by F3 (nationality dropdown dead); created via backend; invite-email flow untested (MailHog)
- [x] 6. Create student ("Brian Kamau", adm QAT/26/742874, Grade 7 East) — same F3 block; auto admission number works
- [x] 7. Create parent + assign student — page 500s without parentRecord (F5); with record: pickers pre-fill correctly, "Add student" assigns (DB confirms)
- [x] 8. Classes / class groups / sections — group created via UI (CBC levels present); class "Grade 7" + section "East" created and listed
- [x] 9. Subjects (+ assign teacher) — "Mathematics" created for Grade 7 (CBC pathway field present); fetch-subjects lists it; assign works (DB confirms Mary→Mathematics); column mislabel F6
- [x] 10. Academic year + terms — year 2026/2027 created; new year auto-seeds a "First" term; 3 terms created; set-current works; terms pages load (were correctly gated before year existed)
- [ ] 11. Exams + marks entry + tabulation + report card
- [ ] 12. Grade systems
- [ ] 13. Fees (categories, fees, invoices, pay)
- [ ] 14. Timetables
- [ ] 15. Syllabi
- [ ] 16. Notices
- [ ] 17. Exit to platform

## FAILS (bugs — must fix)
> Status at end 2026-09-05 session: F1, F2, F4, F5, F6, F7 FIXED. **F3 STILL OPEN** (dropdown; agent-2 failed on rate limit mid-fix, partial edits reverted to clean HEAD). Full test suite has pre-existing test-DB env failures (missing testing tables, migration races, deadlocks) — unrelated to these edits.
| # | Where | What | Severity |
|---|-------|------|----------|
| F1 | Set working school picker (`set-school.blade.php`) + set-term picker | "Set School" button does nothing when clicked — April `x-button` defaults to `type="button"`, so the plain HTML form never submits. No error shown, user stranded on picker. Backend works (direct POST succeeds). Likely affects ~12 forms using `x-button` without `type="submit"` (fee invoices, pay form, profile forms — to verify in sweep) | 🔴 ✅ FIXED |
| F2 | Academic year list (`list-academic-years-table.blade.php:12`) | Duplicate column header: `stop_year` column labelled "Start Year" — should read "Stop Year" | 🟡 ✅ FIXED |
| F3 | Nationality/State dropdowns (`nationality-and-state-input-fields` Livewire + April select) | Dropdown does not open — trigger arrow flips up (state toggles) but the options panel never renders. Likely Livewire DOM-morph vs Alpine conflict. Effect: teacher/parent/admin/student **cannot be created via UI** (nationality+state required). Backend works | 🔴 Critical |
| F4 | `StudentService::createStudentRecord:122` | PHP warning "Undefined array key admission_number" when auto-generating (`$record['admission_number'] \|\| ...` should be `?? null`). Log noise; harmless when debug off | 🟢 ✅ FIXED |
| F5 | Assign-student-to-parent page (`AssignStudentsToParent::mount:37`) | **500 crash** when parent has no `parentRecord` row (`$this->parent->parentRecord->load(...)` on null). Should handle gracefully (empty state / redirect with message) instead of 500 | 🔴 ✅ FIXED |
| F6 | Assign-teacher page (`assign-teacher-to-subjects.blade.php:30`) | Table column header reads "Teacher" above a list of **subjects** — should be "Subject" | 🟡 ✅ FIXED |
| F7 | Livewire v4 POST `/livewire-<hash>/update` via domain | 404 through Caddy (proxy matcher only matched legacy `/livewire/*`, not v4 `/livewire-<hash>/update`); direct 127.0.0.1:8093 = 419 alive. Fixed Caddy matcher + reloaded | 🔴 ✅ FIXED (system file, not in repo) |

## IMPROVEMENTS (not broken, should polish)
| # | Where | Suggestion |
|---|-------|------------|
| I1 | Create school | After creating, user stays on empty form — redirect to schools list (or offer "Enter school") so the flow continues |
| I2 | Create school form | "Please set a school" subtitle is confusing on a create page; logo field gives no hint on accepted file types/size |
| I3 | New academic year | Auto-seeds a term literally named "First" — rename to "Term 1" or leave terms empty for the admin to name |
| I4 | User forms (teacher/parent/admin/student) | Nationality defaults to first country alphabetically, gender/blood/religion preselect first option (Male/A+/Christianity) — preselected values look like user choices; default nationality to Kenya and leave optionals unselected |
| I5 | User forms | "State" label is Nigerian terminology — Kenya uses "County". "Other names *" required forces input where none exists — make optional |
